info:
  name: getAuskunft
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/auskunft/anfrage/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
