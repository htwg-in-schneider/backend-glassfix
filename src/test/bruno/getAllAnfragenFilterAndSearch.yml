info:
  name: getAllAnfragenFilterAndSearch
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8081/api/anfrage?status=ERSTELLT&kundeId=1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
