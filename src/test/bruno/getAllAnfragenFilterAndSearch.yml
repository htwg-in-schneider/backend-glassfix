info:
  name: getAllAnfragenFilterAndSearch
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/anfrage?status=ERSTELLT&kundeId=1"
  params:
    - name: status
      value: ERSTELLT
      type: query
    - name: kundeId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
