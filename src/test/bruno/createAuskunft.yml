info:
  name: createAuskunft
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/anfrage/2"
  body:
    type: json
    data: |-
      {
      "antwort" : "Antwort für Anfrage 1",
      "status" : "PRUEFUNG_ABGESCHLOSSEN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
