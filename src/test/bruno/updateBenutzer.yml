info:
  name: updateBenutzer
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8081/api/benutzer/4
  body:
    type: json
    data: |-
      {
      "benutzername" : "Juan_Cerda_Updated",
      "email" : "juan.cerda@example.com",
      "hashpasswort" : "passwort123",
      "rolle" : "KUNDE",
      "adresse" : "Beispielweg 2, 54321 Beispielstadt",
      "telefonnummer" : "0123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
