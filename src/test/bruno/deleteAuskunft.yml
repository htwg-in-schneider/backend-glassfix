info:
  name: deleteAnfrage
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/auskunft/anfrage/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​