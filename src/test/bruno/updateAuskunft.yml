info:
  name: updateAuskunft
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/auskunft/anfrage/2"
  body:
    type: json
    data: |-
      {
      "zeitEinschaetzng" : 5,
      "arbeitsschritte" : ["Schritt 1", "Schritt 2", "Schritt 3"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
