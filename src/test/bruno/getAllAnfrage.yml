info:
  name: getAllAnfragen
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/anfrage"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
