info:
  name: getFilteredAuskunft
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/anfrage?status=IN_BEARBEITUNG"
  params:
    - name: status
      value: IN_BEARBEITUNG
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
